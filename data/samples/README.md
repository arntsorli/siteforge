# Sample Data

Large terrain files are intentionally not checked in.

Use `apps/api/siteforge_api/providers/hoydedata.py` to fetch live DTM1 GeoTIFF tiles, or place a small test GeoTIFF here for offline development and point `SITEFORGE_SAMPLE_DTM` at it.

